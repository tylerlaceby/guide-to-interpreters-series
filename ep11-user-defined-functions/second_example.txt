let val = 10;

print(val)

fn addVal() {
    val = val + 5

    print(val)
}

fn secondFn() {
    let hrMin = time() / 60000;

    hrMin = hrMin / 525960

    print(hrMin, time())
}

setInterval(
addVal,
secondFn,
3000)
