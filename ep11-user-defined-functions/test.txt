fn makeAdder (offset) {

  fn add (x, y)  {
    x + y + offset
  }

  add
}

const adder = makeAdder(1);

print(adder(10, 5))


let val = 10;

print(val)

fn addVal() {
    val = val + 5

    print(val)
}

setInterval(
addVal
, 1000)
